import React from 'react';import {Head,Link,useForm} from '@inertiajs/react';import Layout from '../../Components/Layout';
export default function Login(){const {data,setData,post,processing,errors}=useForm({email:'',password:'',remember:false});return <Layout><Head title="Login"/><div className="auth-page container-small"><div className="auth-card"><span className="eyebrow">WELCOME BACK</span><h1>Login</h1><form onSubmit={e=>{e.preventDefault();post('/login')}}><label>Email<input type="email" value={data.email} onChange={e=>setData('email',e.target.value)}/>{errors.email&&<small>{errors.email}</small>}</label><label>Password<input type="password" value={data.password} onChange={e=>setData('password',e.target.value)}/></label><button className="button button-primary" disabled={processing}>Login</button></form><p>Don't have an account? <Link href="/register">Create one</Link></p></div></div></Layout>}
