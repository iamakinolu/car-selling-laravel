import React from 'react';import {Head,Link,useForm} from '@inertiajs/react';import Layout from '../../Components/Layout';
export default function Register(){const {data,setData,post,processing,errors}=useForm({name:'',email:'',password:'',password_confirmation:''});return <Layout><Head title="Create Account"/><div className="auth-page container-small"><div className="auth-card"><span className="eyebrow">JOIN THE MARKETPLACE</span><h1>Create Account</h1><form onSubmit={e=>{e.preventDefault();post('/register')}}><label>Name<input value={data.name} onChange={e=>setData('name',e.target.value)}/>{errors.name&&<small>{errors.name}</small>}</label><label>Email<input type="email" value={data.email} onChange={e=>setData('email',e.target.value)}/>{errors.email&&<small>{errors.email}</small>}</label><label>Password<input type="password" value={data.password} onChange={e=>setData('password',e.target.value)}/>{errors.password&&<small>{errors.password}</small>}</label><label>Confirm Password<input type="password" value={data.password_confirmation} onChange={e=>setData('password_confirmation',e.target.value)}/></label><button className="button button-primary" disabled={processing}>Create Account</button></form><p>Already have an account? <Link href="/login">Login</Link></p></div></div></Layout>}
