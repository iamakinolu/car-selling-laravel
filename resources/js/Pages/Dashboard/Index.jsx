import React from 'react';import {Head,Link,router} from '@inertiajs/react';import Layout from '../../Components/Layout';
export default function Index({cars}){return <Layout><Head title="My Cars"/><div className="container section"><div className="section-heading"><div><span className="eyebrow">SELLER DASHBOARD</span><h1>My Cars</h1></div><Link href="/sell" className="button button-primary">Add New Car</Link></div>{cars.data.length?<div className="dashboard-list">{cars.data.map(c=><div className="dashboard-row" key={c.id}><img src={c.image_url} alt=""/><div><h3>{c.maker} {c.model}</h3><p>₦{Number(c.price).toLocaleString()} · {c.year} · {c.published?'Published':'Draft'}</p></div><div className="dashboard-actions"><Link href={`/cars/${c.id}`}>View</Link><Link href={`/cars/${c.id}/edit`}>Edit</Link><button onClick={()=>confirm('Delete this listing?')&&router.delete(`/cars/${c.id}`)}>Delete</button></div></div>)}</div>:<div className="empty-state"><i className="fas fa-car"/><h3>You have no listings yet.</h3><Link href="/sell" className="button button-primary">List a Car</Link></div>}</div></Layout>}
